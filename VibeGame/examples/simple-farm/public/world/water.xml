<!--
  World fragment loaded via Include src= (see index.html shell).
  Edit this file — do not paste into index.html.

  The river runs the length of the map north→south and keeps EAST of the town
  pad (x ∈ [-48, 48]) so it never trenches the settlement floor, then bends west
  across the southern meadow into the lake. Water carves the heightfield AFTER
  the pads (water/context.md), so a course that crossed a pad edge would cut a
  step into it — the whole point of the continuous relief.

  The bridge span in paths.xml sits at z ≈ 0, where the bed is at x ≈ 58.
-->
<Group name="water" pos="0 0 0">
  <River
    path="44 232  50 186  55 140  58 92  60 44  58 0  52 -46  36 -88  4 -118  -38 -136  -78 -142"
    width="6.4"
    depth="2.6"
    water-offset="0.5"
    bank-width="2.2"
    bank-height="0.6"
    color="#2a6d80"
    opacity="0.86"
  ></River>

  <!-- South-west bowl. gen_terrain.py already dips this corner ~4 m, so the
       lake sits in ground that reads as a basin rather than a stamped hole. -->
  <Lake
    at="-104 -150"
    radius="30"
    depth="3.4"
    water-offset="0.55"
    color="#2d7a8c"
    opacity="0.78"
  ></Lake>
</Group>
